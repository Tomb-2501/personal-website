<%- include("partials/header.ejs") %>
    <% if(locals.job) { %>
        <div class="container py-4">
            <div class="row p-4 p-md-5 mb-4 rounded text-body-emphasis bg-body-secondary">
                <div class="col-auto mx-auto">
                    <img class="rounded-3" src="<%= job.logo %>" alt="The company's logo" height="240" />
                </div>
                <div class="col-lg-6 px-0">
                    <h1 class="display-3">
                        <%= job.title %>
                    </h1>
                    <h5 class="text-secondary">
                        <%= job.intro %>
                    </h5>
                    <p class="lead my-3">
                        <%= job.date %>
                    </p>
                </div>

            </div>
            <div class="container my-5">
                <div class="row p-4 pb-0 pe-lg-0 pt-lg-5 align-items-center rounded-3 border shadow-lg">
                    <div class="col-lg-7 p-3 p-lg-5 pt-lg-3">
                        <h1 class="display-4 fw-bold lh-1 text-body-emphasis">
                            My Experience
                        </h1>
                        <p class="lead">
                            <%= job.content1 %>
                        </p>
                        <p class="lead">
                            <%= job.content2 %>
                        </p>
                        <p class="lead">
                            <%= job.content3 %>
                        </p>

                    </div>
                    <div class="col-lg-4"><img class="pb-5 px-auto rounded-3 mx-auto img-fluid"
                            src="<%= job.picture1 %>" alt="<%= job.alt %>"></a>

                    </div>
                </div>
            </div>

        </div>
        <% } %>
            <%- include("partials/footer.ejs") %>